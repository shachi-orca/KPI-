# KPI管理システム / CRM — Claude Code 引き継ぎプロンプト

## ⚡ 次のセッションで最初にやること

着手前に必ず対象ファイルを Read して現在の行数・関数配置を確認すること。

## 📌 現在の状態（2026-06-21時点・最新コミット `(本コミット)`）

- **無料で対応可能な改善項目はすべて完了・コミット済み**（main最新）。CTI/Web会議以外は契約待ちなし
- 納期目安：**2026-06-27頃**（2026-06-20時点でユーザーより「納期まで1週間」と言及）
- パスワード平文保存は「管理者がスタッフPWを直接確認できる運用を優先する」という判断で**現状維持に確定**（再検討不要・理由は下記表参照）
- CTI／Web会議の契約が決まったらユーザーに確認の上、本格連携に着手する
- **依頼数・後確数・契約数・解約数の手動入力対応が完了**（2026-06-21）。CRM自動加算（`incrementKpiCount()`）と手動入力は同一フィールド（`kpiData[期間][si][day][指標]`）を共有しており、ユーザーが選択した「手動入力値にCRM加算分を上乗せ」方式をそのまま実現できた（後述セクション参照）

---

### 【次優先】未対応事項

| ファイル | 内容 | 状態 |
|---|---|---|
| `customer_crm.html` | CTI（電話）本格連携：ソフトフォン/CTIサービス契約後、`callNumber()`をAPI呼び出しに差し替え | **契約待ち** |
| `customer_crm.html` | Web会議自動生成連携：Zoom等のAPI契約後、`openMeeting()`周りに会議自動作成処理を追加 | **契約待ち** |

現時点で無料対応可能な未対応事項はなし（上記2件は契約待ちのみ）。

### 確定済み・再検討不要の決定事項

| 項目 | 決定内容 | 理由 |
|---|---|---|
| `kpi_system.html` パスワードの平文保存 | 現状維持（2026-06-20確定） | 管理者がスタッフPWを直接確認できる運用を、ハッシュ化によるセキュリティ向上より優先する判断。クライアントのみの静的アプリのため、暗号化してもキーがソースに埋め込まれ実質的な強度向上にならない点も考慮済み |

---

## 今セッション（2026-06-21・続き4）で完了した実装：依頼数・後確数・契約数・解約数の手動入力対応

前セッションでCRM自動集計専用（読み取り専用）にした4項目について、ユーザーから「手動でも対応できるように」との要望があり対応。共存方式は事前にユーザーに確認し、**「手動入力値にCRM加算分を上乗せ」**を選択。

### `kpi_system.html` 実装内容 ✅

- `CRM_LINKED_KPI=['依頼数','後確数','契約数','解約数']`の判定によってセルを読み取り専用にしていた2箇所を変更し、通常の`<input>`に戻した：
  - KPI入力ページ（個人表示）の日別セル — `CRM_LINKED_KPI`の専用分岐を削除し、通常の編集可能`<input>`分岐に統合。CRM連携項目であることが分かるよう`title`属性のヒント（「CRMの対応履歴ステータス変更でも+1で自動加算されます」）のみ残した
  - 本日入力（個人ダッシュボード）テーブル — `readOnly`判定を`computed`（`COMPUTED_KPI`＝コンタクト数）のみに変更し、`linked`（CRM連携4項目）は編集可能な`<input data-rt-si><input data-rt-kpi>`側に統合。同様に`title`ヒントを付与
  - `CRM_LINKED_KPI`の定数自体・案内バナー文言・コメントは「手入力も可能。CRM加算は手入力値に+1で上乗せ」という新仕様に合わせて更新
- **実装上のポイント**：`customer_crm.html`の`incrementKpiCount()`は元々`kpiData[期間][si][day][指標]`に直接`+1`するだけの単純な加算処理で、手動入力の`oninput`ハンドラも同じフィールドに値を書き込む。**両者が同じストレージフィールドを共有しているため、読み取り専用の分岐を外してinputに戻すだけで「手動入力値にCRM加算分を上乗せ」が自然に実現される**（ロジック変更は不要）。手動で入力した値がベースとなり、その後CRM側の対応ステータス変更で+1されていく仕組み
- `isKpiBelowThreshold`の赤色判定ロジックは変更不要（既存のまま依頼数等にも正しく適用される）

**動作確認済み（headless Chrome／CDP経由、一時的にpuppeteer-coreを`/tmp`配下にのみ導入し検証専用の独立プロファイルで起動。リポジトリ・ユーザーの実プロファイルには影響なし、検証後にプロセス停止・ファイル削除済み）**：
1. KPI入力ページで依頼数・後確数・契約数・解約数の4項目が`<input>`（編集可能）に戻っていること、コンタクト数は引き続き読み取り専用のままであることをDOM構造で確認
2. 本日入力テーブルでも同4項目が`<input data-rt-kpi="依頼数">`等の編集可能要素になっていることを確認
3. 依頼数に手動で「5」を入力→保存（debounce後）→`localStorage`の`kpiData`に5が反映されることを確認
4. `customer_crm.html`の`incrementKpiCount()`と同等の処理をlocalStorage側に直接適用（+1）→リロード後、依頼数の入力欄に「6」と表示され、かつ編集可能なまま維持されることを確認（手動値+CRM加算の上乗せが成立）

---

### 解決済み：案件管理の顧客選択バグ → 手動入力機能を削除する方針に確定（2026-06-19）

前回セッションでKGI独自の「顧客リスト」ページを削除した際、`state.customers` を読み込む手段がなくなり、`dealModal`の顧客選択が常に空になる不具合があったが、ユーザー判断で「KGIはKPI・CRMからの自動転記の確認のみで良く、手動入力は不要」と方針確定。`dealModal`/`showDealModal`自体を削除して解決済み（詳細は後述）。

---

## 今セッション（2026-06-21）で完了した実装：6月本番リハーサル用テストデータ・達成率表示修正・CRM↔KPI連携・スタッフ無効化・コンタクト数ファネル追加・赤色判定基準

### ① 6月の実稼働を想定したテストデータ生成（リポジトリには含まない・gitignore対象）

ユーザー要望：本番稼働前に1ヶ月分の架空データで動作確認したい。後半10日間で契約数が目標に着地するシナリオで作成。

- `gen_test_data_202606.js`：生成スクリプト（Node）。前半20日は契約数を目標の最大40%までしか積まず、後半10日（実働7日）で残りを後ろ重みで積み上げ、チーム合計が目標300件にぴったり一致するよう構築。ファネル上流≧下流の整合性をアサーションで検証済み
- `kpi_backup_test_202606.json` / `crm_backup_test_202606.json`：kpi_system.html / customer_crm.html それぞれのバックアップ復元用JSON（デフォルトスタッフ10名・デフォルトPWで新規構築。restore時に既存データを完全上書きする点はユーザー了承済み）
- headless Chrome（CDP）で実際に画面表示まで確認済み（個人ダッシュボードの日別契約数が後半に向けて加速する様子・CRMポータルの表示等）
- 生成スクリプト・出力JSONはいずれも`.gitignore`登録済み（リポジトリには入らない。再生成したい場合は`node gen_test_data_202606.js`を再実行）

### ② 達成率表示の100%キャップ解消

個人ダッシュボード・チームダッシュボード・KPI入力Total表示・業績サマリー・着地確認(timeline)・目標設定など、「実績÷目標」の達成率表示が**実績が目標を超えても100%で丸められていた**箇所を、実数（115%、122%等）を表示するよう修正。進捗バーの見た目（width）だけは100%でクランプし崩れないようにしている。応答率・契約率等の「比率」系指標（0〜100%の意味を持つもの）は従来通り100%キャップを維持。

### ③ CRM（customer_crm.html）↔ KPI（kpi_system.html）連携の強化

ユーザー要望：「CRMの活動結果がKPI管理に反映されるようにしたい」。

- `customer_crm.html`の対応ステータス（`STATUSES`）に **依頼中・後確中・契約中・解約中** を追加（対応中と完了の間に挿入。解約中は完了から遷移できる別枠）。バッジ色（`.br`/`.bc`/`.bk`/`.bx`）も追加
- `saveContact()`で対応履歴を保存するたびに、選択した対応ステータスに応じて`incrementKpiCount()`がkpi_system.html側のkpiSystemState_v2を直接書き換え、該当日・該当スタッフの依頼数/後確数/契約数/解約数に+1（**保存するたび毎回+1。同一顧客への複数回対応で重複加算される仕様** — ユーザーが明示的に選択した仕様）
- kpi_system.html側は`CRM_LINKED_KPI=['依頼数','後確数','契約数','解約数']`をこの4項目に指定し、KPI入力ページ・本日入力（ダッシュボード）の該当セルを読み取り専用（紫色）に変更
- **⚠️2026-06-21の別要望で「手動入力もできるようにしてほしい」と方針転換あり。上記【次優先】参照**
- 既存のCRM→KGI連携（顧客status='完了'→`crm_system.html`に成約として自動転記）は変更なし。「KPIの契約数」と「CRMの顧客status=完了」は別概念のまま（ユーザー確認済み）

### ④ スタッフの「無効化」機能（完全削除との2パターン化）

ユーザー要望：スタッフ数を減らしてテストしたいが、データは消したくない。完全削除以外の選択肢が欲しい。

- `state.staffInactive`（無効化index配列）を追加。`isStaffActive(si)`/`toggleStaffActive(si)`で判定・切替
- 管理者画面→スタッフ管理タブに「無効化」ボタン（🟠人アイコン）を追加。既存の「完全削除」（🔴ゴミ箱）はそのまま維持
  - **無効化**：データ保持・ログイン不可・現在チーム表示（チームダッシュボード/アラート/本日の出勤状況/代理打刻選択/管理者ビュー等）から除外。**いつでも元に戻せる**
  - **完全削除**：既存動作通り、全期間データも削除（不可逆）
- ログイン画面（kpi_system.html / customer_crm.html / crm_system.html の3つすべて）の担当者選択から無効化スタッフを除外
- チーム目標合計・実績合計（`goalSumActive()`/`sumKpiAllActive()`という新ヘルパーを追加）は無効化スタッフを除いて計算。一方、個人別のドリルダウン画面（KPI入力・出勤履歴・業績サマリー・着地確認・目標設定の個人選択等）では無効化スタッフも引き続き選択・閲覧可能（データ保持の趣旨に合わせた）
- CSV出力は全期間・全スタッフを含む完全な履歴のまま（無効化の影響を受けない）

### ⑤ 「コンタクト数」のファネル追加と完了率の再定義

ユーザー要望：コール数・アプローチ数の次にコンタクト数を追加し、ファネル計算を見直したい。

- `KPI_ACT`に**コンタクト数**を追加（アプローチ数の次・対コール/対アプローチコンタクト数の前）。値は「対コールコンタクト数＋対アプローチコンタクト数」の自動合算（`COMPUTED_KPI=['コンタクト数']`指定で手入力不可・紫色セル）。`genDayData()`内で`_noSample`に関わらず常に再同期される（手入力データにも遡って適用）
- 完了数の達成基準は**コンタクト数**を分母にする（「コンタクトした案件のうち失注・成約問わず完了に至った件数」という定義。ユーザー確認済み）
- 新しい率指標「**対コンタクト完了率**」（完了数÷コンタクト数）を`KPI_RATE`・`RATE_MAP`・`calcRate()`/`calcSlotRate()`・業績サマリーの率カードに追加
- 目標設定ページのコンタクト数目標は対コール/対アプローチの目標合算で自動表示（`syncContactGoal()`、手入力不可）
- **既存の潜在バグを合わせて修正**：KPI項目名をDOM id化する際`replace(/[^\w]/g,'_')`で日本語を全て`_`に変換していたため、文字数が同じ項目（完了数/依頼数/後確数/契約数/解約数は全て3文字→`___`で衝突、コンタクト数追加でアプローチ数とも6文字で衝突）でリアルタイム更新が誤った要素を書き換える可能性があった。`kpiDomId(k)`（KPI_ACT配列のindexベース）に統一して解消

### ⑥ KPI入力の赤色判定基準（管理者設定可）

ユーザー指定の10基準をすべて実装：

| 項目 | 基準（デフォルト値） |
|---|---|
| コール数 | 稼働1時間あたり30件未満で赤 |
| アプローチ数 | 稼働1時間あたり1件未満で赤 |
| 対コールコンタクト数 | コール数の30%未満で赤 |
| 対アプローチコンタクト数 | アプローチ数の50%未満で赤 |
| 完了数 | コンタクト数の30%未満で赤 |
| クロージング数 | 完了数の18%未満で赤 |
| 依頼数 | 稼働4時間で1件のペース未満で赤 |
| 後確数 | 依頼数の98%未満で赤 |
| 契約数 | 後確数の98%未満で赤 |
| 解約数 | 1日2件以上で赤 |

- `state.kpiThresholds`（未設定キーは`DEFAULT_KPI_THRESHOLDS`を使用）・`isKpiBelowThreshold(si,day,k)`で判定。稼働時間は`workedHoursFor(si,day)`でその日の実際の打刻（休憩除く実働）から算出し、**未打刻の日は判定しない**（通常の黒色のまま）
- 管理者画面に新タブ「**赤色判定基準**」を追加（`adminThreshold()`）。全10項目を保存・デフォルトに戻す操作が可能
- KPI入力ページ・本日入力（ダッシュボード）の対象セル（手入力中・CRM連携中・コンタクト数の計算セルいずれも）に赤字で反映。本日入力のリアルタイム更新（対コール/対アプローチコンタクト数編集時のコンタクト数再同期・完了数/クロージング数の赤色再評価含む）にも対応
- headless Chromeで実際の画面表示・計算結果（例：完了数8件÷コンタクト29件=27.6%<30%→赤、10件÷28件=35.7%→黒）を確認済み
- **既存のテストデータ（kpi_backup_test_202606.json）はコール数が1日20〜60件程度の想定で作られているため、「時間あたり30件」基準では常に赤色判定になる**（テストデータの想定件数がこの基準より小さいだけで、ロジックの不具合ではない。実データでは件数に応じて正しく切り替わる）

---

## 今セッション（2026-06-20・続き3）で完了した実装：目標トレンドグラフ／年範囲／ファネル0偏りの解消

旧バックログ3点（目標の複数月トレンドグラフ・期間ナビの年範囲ハードコード・ファネル下流の0偏り）を仕様確認の上で対応。パスワード平文保存は「管理者がスタッフPWを直接確認できる運用」を優先する判断のまま現状維持で確定。

### `kpi_system.html` 実装内容 ✅

- **目標トレンドグラフ（直近6ヶ月・実績vs目標）**：個人ダッシュボード（`dashboardPage()`の個人ビュー、KPIは既存の`state.dashKpi`セレクタと連動）とチームダッシュボード（`teamPage()`、KPIは既存の`state.teamKpi`と連動）の両方に追加。Chart.jsの折れ線グラフ（既存の日別グラフと同じビジュアル規則：実績=青の塗りつき線、目標=灰色の破線）
  - `trendMonths(n)`：閲覧中の月から遡ったnヶ月分の`[年,月]`配列を生成
  - `periodGoal(yy,mm,si,kpi)`/`periodGoalAll(yy,mm,kpi)`：既存の`periodSum`/`periodSumAll`と対になる、任意期間の目標値取得
  - `ensurePeriodGoals(yy,mm)`：既存の`ensurePeriodData`と対になる、任意期間の目標サンプルを冪等に用意（未訪問の過去月でも目標値が0にならないようにするため）。サンプル値生成ロジックは`initKpi()`内に重複していた目標初期値オブジェクトを`goalBaseSample()`として共通化し、両方から呼び出す形に整理
  - 描画は既存の`dash-chart`と同じ「`setTimeout(...,80)`後にCanvas取得して`Chart`生成、`state.trendChart`に保持、`render()`冒頭で毎回destroy」というパターンに統一
- **期間ナビの年範囲ハードコード解消**：`periodNavHTML()`の年セレクトを`REAL_Y-2〜REAL_Y`の固定3年から、`state.kpiData`/`state.goals`に実在するキーの年＋当年＋選択中の年を動的に集計する方式に変更。データが存在する限り何年でも遡れる（ハードコードされた上限なし）
- **ファネル下流の0偏り解消**：`rndBelow(upper,pctMax,floorMin)`に第3引数`floorMin`を追加。完了数〜契約数（`done`/`closing`/`req`/`confirm`/`contract`）の生成にのみ`floorMin=true`を渡し、**上流が1以上ある限り下流の結果を最低1に補正**（上流が0の場合は0のまま、ファネルの整合性は維持）。既存の`cCont`/`aCont`等の呼び出しは引数を変えていないため影響なし。サンプル生成（`genDayData`内の`if(v['コール数']===undefined)`ガード）にのみ適用されるため、既存の保存済みデータや実際の入力値には影響しない

**動作確認済み（2026-06-20、headless Chrome／CDP）**：
- トレンドグラフ：個人/チーム両ダッシュボードでCanvas生成・6ヶ月分のラベルと実績/目標データが正しく入ること、KPI切替で再描画されることを確認
- 年範囲：データのある年のみ選択肢に出ること、過去月へジャンプして新たにサンプル生成されるとその年も選択肢に追加されることを確認
- ファネル0偏り：`genDayData()`を2000回試行し契約数の0発生率が0%に低下（修正前は高頻度で0だった）、かつ上流≧下流のファネル順序違反が0件、上流が真に0のケースでは下流も0のまま（人為的な値の水増しがないこと）を確認

---

## 今セッション（2026-06-20・続き2）で完了した実装：録画ファイルの対応履歴内再生（IndexedDB化）

要望に対する以前の制約「録画ファイルはダウンロードのみで対応履歴から再生できない」を解消。録画データをIndexedDBに保存し、対応履歴から直接再生できるようにした。

### `customer_crm.html` 実装内容 ✅

- IndexedDBラッパー（`idbOpen`/`idbPutRecording`/`idbGetRecording`/`idbDeleteRecording`）を追加。DB名`crmRecordings`、ストア`recordings`（容量の大きいBlobをlocalStorageに入れないため別ストレージを使用）
- `stopRecording()`：録画停止時に`recId`（`'rec'+Date.now()`）を生成し、Blobを`idbPutRecording()`でIndexedDBに保存。**既存の自動ダウンロードは維持**（IndexedDBの保存に失敗してもファイルは手元に残る設計）。`_pendingRecId`にrecIdを保持
- `saveContact()`：対応履歴保存時に`recId:_pendingRecId||null`をcontactオブジェクトに付与。保存後`_pendingRecId=null`でリセット
- 対応記録キャンセル（`pl-cancel`）時も`_pendingRecId=null`でリセット（孤立したIndexedDBエントリは残るが実害なし）
- タイムライン：`h.recId`がある履歴項目に「録画を再生」ボタンを表示
- `playRecording(recId)`：IndexedDBから取得→`Blob`→`URL.createObjectURL`→モーダルで`<video controls autoplay>`再生。見つからない場合はエラートースト
- `deleteRecording()`：IndexedDBエントリ削除＋該当contactの`recId`をnullに戻す（即時実行・確認ステップなし。既存の`deleteApo()`等と同じ単発削除の慣習に合わせた）
- モーダルを閉じる際（`mov`背景クリック/`mcancel`/削除後）は`closeRecModal()`で必ず`URL.revokeObjectURL()`を実行しメモリリークを防止

**動作確認済み（2026-06-20、headless Chrome／CDP、`getDisplayMedia`/`MediaRecorder`をモック）**：録画→停止（ダウンロード＋IndexedDB保存）→対応記録保存（recId紐付け）→タイムラインに再生ボタン表示→再生モーダルでBlob URLの`<video>`が表示→削除でIndexedDBエントリと履歴側のrecId参照が両方消えることを、一連の流れで確認済み。

**既知の制約**：IndexedDBのストレージ容量はブラウザ依存（端末のディスク空き容量に応じて自動拡張されるが上限はある）。容量管理用のUI（使用量表示・一括削除など）は未実装。録画件数が多くなった場合は今後検討。

---

## 今セッション（2026-06-20・続き）で完了した実装：バックアップ復元機能

要望：バックアップを取るだけでなく元に戻せないと安全網として不完全との指摘から、3ファイル共通でリストア機能を追加。

### 実装パターン（3ファイル共通）

- 既存の「データバックアップ」セクション内（kpi_system.html: 管理者→スタッフ管理タブ末尾／crm_system.html: 管理者ビュー新規カード／customer_crm.html: 設定→データ管理タブ）に「バックアップから復元」UIを追加
- `_restorePending`（グローバル変数、`{text, name}`）：ファイル選択→検証通過後にセットされ、確認待ち状態を表す
- `onRestoreFileSelected(file)`：`FileReader`でテキスト読込→`JSON.parse`失敗時はエラーtoast→ファイルごとの簡易スキーマ検証（kpi: `kpiData`/`staffPw`の存在、KGI: `deals`の存在、customer_crm: `customers`/`contacts`の存在）→通過したら`_restorePending`にセットして`render()`
- 検証通過後、`confirm()`は使わず既存の「クリア実行/キャンセル」と同じインラインの警告バナー＋「復元実行」「キャンセル」ボタンで確認（誤操作防止）
- `confirmRestore()`：該当のlocalStorageキーに生テキストをそのまま書き込み→`location.reload()`
- `cancelRestore()`：`_restorePending=null`で取り消し

**動作確認済み（2026-06-20、headless Chrome／CDP）**：3ファイルそれぞれで、(1)不正なJSON・スキーマ不一致のファイルが拒否されること、(2)正常なファイル選択で確認バナーが表示されること、(3)キャンセルで取り消せること、(4)復元実行でlocalStorageが書き込まれ、リロード後に内容が正しく反映されること、を確認済み。

---

## 今セッション（2026-06-19・後半）で完了した実装：CTI/Web会議連携の下準備

ユーザー要望：CRMから顧客情報をもとに電話・メール発信、オンライン商談（Web会議）をできるようにしたい（昔のFileMaker+Zoiperのような使い方のイメージ）。
今回はソフトフォン・会議サービスをまだ契約していないため、契約後すぐ繋ぎ込めるよう「ベース」のみ実装。本格CTI API（Twilio等）・会議自動生成API（Zoom等）は未実装（契約後に着手）。

### `customer_crm.html` 実装内容 ✅

- `callNumber(tel)` / `composeMail(email)` / `openMeeting(url)` を追加（utilities関数群の直後）。現状は`tel:`/`mailto:`リンクと`window.open`のみだが、**将来CTI/会議APIに差し替える際はこの3関数だけ変更すればよい設計**。
- 顧客詳細ページ：電話番号・連絡先に「発信」ボタン（`data-call`）、メールに「メール作成」ボタン（`data-mail`）を追加。クリックでOS既定のアプリ（Zoiper等をtel:ハンドラに設定していればそれが起動）が立ち上がる。
- アポイント設定フォームに「Web会議URL（任意）」入力欄（`apo-mtg`）を追加。`http(s)://`形式のみ許可（簡易バリデーション）。
- アポイントデータに`mtg`フィールドを追加（既存データへの影響なし）。設定されている場合、次回アポイント表示エリアに「Web会議に参加」ボタン（`data-meeting`）を表示。
- バインド：`data-call`/`data-mail`/`data-meeting`を`bind()`内に追加。

### 既存バグ修正：アポイント編集時の日時表示がズレる（2026-06-19発見・修正済み）

`detailHTML()`の`apoVal`が`new Date(apo.at).toISOString().slice(0,16)`（UTC基準）を`datetime-local`入力欄にそのまま入れていたため、JST環境では実際の予定時刻より9時間早い日時が表示されていた。既存の有効なアポイントを編集して保存すると、日時を変更していなくても「過去の日時は設定できません」エラーで保存できない、または意図せず時刻がズレる不具合があった。
`toLocalDT(ts)`関数を新設（ローカル時刻基準でフォーマット）し、`apoVal`の計算をこれに置き換えて修正。**今回のWeb会議URL機能のテスト中に発覚**——既存アポイントにURLを追記しようとすると保存できない事象から特定。

**動作確認**：headless Chrome（CDP経由）で、(1)発信/メール作成/会議参加ボタンの表示とクリック時の関数呼び出し、(2)Web会議URLの不正値拒否・正常値保存・空値クリア、(3)修正後は既存アクティブなアポイントの編集保存が正常に通ることを確認済み。

### 追加要望：対応記録の保存を強制するナビゲーションロック ✅

要望：発信/メール作成/Web会議参加ボタンを押したら、新規対応入力を保存するまで他ページに移動できないようにする。

実装：
- `pendingLog`（グローバル変数。`{type:'call'|'mail'|'meeting'}`）を追加。`data-call`/`data-mail`/`data-meeting`クリック時にセットし`render()`。
- `blockNav()`：`pendingLog`があれば`toast`でエラー表示し`true`を返すガード関数。`logout-btn`/`data-nav`/`back-btn`の各クリックハンドラの先頭で呼び出し、`true`なら処理を中断（ページ遷移しない）。
- 顧客詳細ページ上部に警告バナーを表示（`pendingLog`がある間）。誤クリック時の救済として「対応記録をキャンセル」リンク（`pl-cancel`）を設置——確認なしの完全ブロックだと誤操作時に詰むため追加。不要なら削除可。
- `saveContact()`成功時に`pendingLog=null`でロック解除。
- `window.beforeunload`でタブを閉じる/リロードする際もブラウザ標準の確認ダイアログを表示（`pendingLog`がある場合のみ）。

**動作確認**：headless ChromeでCDP経由で、(1)発信後に設定/案件管理タブ・戻るボタン・ログアウトが全てブロックされること、(2)対応記録を保存すると解除され遷移できること、(3)「キャンセル」リンクでも解除できることを確認済み。

---

## 今セッション（2026-06-19・後半その2）で完了した実装：① 案件追加モーダル削除 ／ ③ データバックアップ

### ① crm_system.html：案件管理の手動入力を完全削除 ✅

方針確定：KGIには「KPI・CRMからの自動転記の確認」だけが残ればよく、手動での案件入力・編集は不要（ユーザー指示）。

削除したもの：
- `dealModal()` / `showDealModal()` 関数（「案件追加」「編集」両方）
- 「案件追加」ボタン（`deal-add-btn`）、編集ボタン（`data-deal-edit`）とそのバインド
- `dealRow`の「顧客」列・「確度」列（`state.customers`参照は常に空配列で動作していなかったため。タイトルに顧客名が既に含まれているので実害なし）
- `adminPage()`の「顧客数」列
- `state`から`customers`・`selCustId`・`custFilter`（前回の顧客リスト削除時の残骸、完全に未使用だった）

結果：案件は完全に「CRMの完了顧客→自動転記」のみで作られ、表示専用になった。

### ③ 3ファイル共通：自動バックアップ機能 ✅

要望：データが大きくなってきたので壊れる前にバックアップしたい。外部に漏れない方法で、夜間・朝などの「決まった時刻」自動実行を希望されたが、静的HTML（サーバーなし）では「タブが閉じていても決まった時刻に実行」は技術的に不可能なため、下記の方式に決定：

- **ログアウトボタンを押した瞬間に自動バックアップ**（実質「閉じる前にバックアップ」と同等。`beforeunload`中のダウンロードはブラウザにブロックされるため不採用）
- **ログイン後、前回バックアップから12時間以上経過していたら自動バックアップ**（ログアウトせず閉じた場合の保険）
- 保存先は**完全にローカルのみ**（ブラウザの自動ダウンロード。外部送信なし）

実装（3ファイル共通パターン、関数名統一）：
- `BACKUP_KEY`／`BACKUP_INTERVAL_MS`（12時間）／`_backupChecked`（1ページロードに1回だけチェック）
- `lastBackupLabel()`：最終バックアップ日時を表示用に整形
- `exportFullBackup(silent)`：全データをJSONダウンロード＋`BACKUP_KEY`に時刻保存。`silent=false`なら保存完了トースト表示
- `checkAutoBackup()`：`BACKUP_INTERVAL_MS`超過していれば`exportFullBackup(true)`
- `render()`内で認証済み描画の直前に1回だけ`checkAutoBackup()`を呼ぶ
- ログアウトボタンのonclick先頭で`exportFullBackup(true)`を呼んでからセッションクリア

ファイル別の詳細：
| ファイル | 手動バックアップボタンの場所 | バックアップ対象 |
|---|---|---|
| `kpi_system.html` | 管理者画面 → スタッフ管理タブ末尾 | `localStorage['kpiSystemState_v2']`全体 |
| `crm_system.html` | 管理者ビュー（ページ末尾に新規カード） | `localStorage['crmSystemState_v1']`全体 |
| `customer_crm.html` | 設定 → データ管理タブ（既存の「すべてのデータをまとめて保存」ボタンを`exportFullBackup`経由に変更） | `D`（顧客・履歴・商材・割り当て）全体 |

**動作確認**：headless Chrome（CDP経由）で3ファイルそれぞれ、(1)初回ログイン時に自動バックアップが走ること、(2)同セッション内の再renderで重複実行されないこと、(3)手動バックアップボタンが動作すること、(4)ログアウト時に自動バックアップ後セッションがクリアされることを確認済み。

---

## 今セッション（2026-06-20）で完了した実装：通話・Web会議の録画機能

要望：応対履歴で電話やオンライン商談を録画して、後から対応履歴から確認できるようにしたい。
電話の録画はブラウザからは技術的に不可能（OS/電話アプリ側の機能が必要）なため対象外。Web会議の録画は「有料の会議サービスの録画機能」と「無料のブラウザ標準画面録画」の二択を提示し、**無料の方（ブラウザ標準）を採用**。

### `customer_crm.html` 実装内容 ✅

- `navigator.mediaDevices.getDisplayMedia()` + `MediaRecorder`でブラウザ標準の画面録画を実装（契約・追加コストなし）
- 顧客詳細ページの「新規対応入力」パネル内に録画開始/停止ボタンを追加
- 録画停止時：`.webm`ファイルを自動ダウンロード（ファイル名: `録画_顧客名_YYYYMMDD_HHMM.webm`）し、対応入力欄に`[録画ファイル: ファイル名]`を自動追記
- 録画中・対応記録未保存時はナビゲーションロック（`blockNav()`）が発動し、他ページへ移動できないようにした（既存の`pendingLog`機構を拡張、`type:'record'`を追加）
- `beforeunload`にも`isRecording`チェックを追加（録画中のタブを閉じようとすると確認ダイアログ）

**動作確認済み（2026-06-20、headless Chrome／CDP、`getDisplayMedia`/`MediaRecorder`をモック）**：(1)録画開始でUI(録画中表示・停止ボタン)が切り替わること、(2)録画中は`blockNav()`が全ページ遷移をブロックすること、(3)停止で`.webm`ダウンロード・対応入力欄への追記・`pendingLog`セットが正しく行われること、(4)保存前は`pendingLog`によりナビゲーションロックが継続すること、(5)`saveContact()`で対応履歴に記録され、ロックが解除されること、を一連の流れで確認済み。

**既知の制約**：
- 動画ファイルはローカルダウンロードのみで、対応履歴上では再生できない（ファイル名の記録のみ）。履歴から直接再生したい場合はIndexedDB保存＋アプリ内プレイヤーへの設計変更が必要（要否はユーザー判断待ち）
- Mac環境では`getDisplayMedia`の音声キャプチャがタブ共有以外（画面全体・他アプリウィンドウ）だとOS側の制約で音声が含まれない場合がある（ブラウザの仕様上の制約）

---

## 前回セッション（2026-06-19・前半）で完了した実装

### ① crm_system.html 活動記録ページ削除 ✅

削除したもの：
- サイドバーの「活動記録」ナビ
- `ACT_TYPES`定数 / `actChip()`関数
- `exportActivityCSV()` / `activitiesPage()` / `actModal()` / `showActModal()` 関数
- `bindPage()`内の活動関連バインド（`act-filter-type`,`act-filter-staff`,`act-csv-btn`,`act-add-btn`,`data-act-del`）
- `state.activities` / `state.actFilter`
- `dashboardPage()`の「最近の活動」カード
- `adminPage()`の「今月活動数」列

### ② crm_system.html CRM自動転記 ✅

- `loadCrmData()`：`crmCustomerState_v1`（customer_crm.html）を読み込み
- `syncDealsFromCrm()`：顧客`status==='完了'`の顧客を`state.deals`に`status:'won'`で自動追加
  - 重複防止：`d.crmSourceId === 'crm_'+cust.id`
  - 商材名：該当顧客の最新contactの`pid`→`products`
  - 担当者：最新contactの`si`
  - `render()`先頭で`loadCrmData()`→`syncDealsFromCrm()`を呼び出し

### ③ crm_system.html KGIダッシュボード連携 ✅

②で自動転記された案件は`state.deals`に乗るため、既存の`monthPrediction()`が自動集計。
ダッシュボードの月末着地予測カードに「（うちCRM自動転記 n件）」の注記を追加。

結果：810行 → 698行（活動記録機能の削減分が転記機能の追加分を上回る）

**動作確認**：headless Chrome（CDP経由）でCRM顧客データを模擬投入し、①サイドバーから活動記録が消えていること②自動転記でstate.dealsに1件追加されること③再renderしても重複しないこと④ダッシュボード・案件管理（フィルタ「全て」時）・管理者ビューに正しく反映されることを確認済み。

---

## 前々回セッション（2026-06-18）で完了した実装

### KGI（crm_system.html）顧客リストページ削除 ✅

削除したもの：
- サイドバーの「顧客リスト」ナビ
- `customersPage()` 関数（48行）
- `customerDetailPage()` 関数（65行）
- `custModal()` 関数（34行）
- `showCustModal()` 関数（23行）
- 関連イベントバインド（顧客リンク・フィルタ・追加・編集）
- 案件・活動ページの顧客名リンク → プレーンテキスト化

結果：996行 → 810行

---

## 3ファイルの役割（確定）

| ファイル | 役割 | 管理する内容 |
|---|---|---|
| **kpi_system.html** | 日々の活動量の記録・管理 | コール数・勤怠・シフト・KPI数字 |
| **customer_crm.html** | 顧客情報マスタ＋対応履歴 | 誰に・何をしたか。成約・失注の記録 |
| **crm_system.html（KGI）** | 結果の確認のみ | 月末着地・契約結果（KPI＋CRMデータを参照） |

KGIに残すページ：ダッシュボード、案件管理、管理者ビュー（3ページのみ）
KGIから削除したページ：顧客リスト（2026-06-18）、活動記録（2026-06-19）

---

## プロジェクト概要

### ファイル構成・行数

```
kpi_system.html     （KPI・シフト・勤怠管理。LocalStorageキー: kpiSystemState_v2）約4061行
crm_system.html     （KGI管理システム。LocalStorageキー: crmSystemState_v1）約685行
customer_crm.html   （顧客管理CRM。LocalStorageキー: crmCustomerState_v1）約1163行
```

### KPI ↔ KGI(crm_system.html) の共有設計
- `crm_system.html` は起動時に `kpiSystemState_v2` を読み込み、`_staffList`・`staffPw`・`adminPw`・`goals`・`role` を参照（書き込みなし）
- KPIシステムで管理者ログイン中にKGIを開くと自動的に管理者セッションを同期
- スタッフがKGIにアクセスするとアクセス拒否画面を表示

### KGI ↔ CRM(customer_crm.html) のデータ連携（2026-06-19追加）
- `crm_system.html` は起動時に `crmCustomerState_v1` を読み込み（`loadCrmData()`）
- 顧客ステータスが「完了」の顧客を`state.deals`に成約として自動転記（`syncDealsFromCrm()`、書き込みは`crmSystemState_v1`側のみ。CRM側は読み取り専用）

### KPI ↔ CRM(customer_crm.html) の設計
- `customer_crm.html` は `kpiSystemState_v2` を認証のみに使用（スタッフリスト・パスワード参照）
- セッション自動同期なし。KPIにログイン済みでも **CRMへの再ログインが必要**
- `?cid={id}` クエリパラメータで顧客詳細に直接遷移

---

## customer_crm.html 設計詳細

### データ構造
```js
D = {
  products:     [{id, name}],
  assignments:  [{id, si, year, month, pid}],
  customers:    [{id, name, company, role, tel, tel2, email,
                  postalCode, address, status, tags, ctx[], createdAt}],
  contacts:     [{id, cid, si, pid, year, month, at,
                  method, reason, manager, content}],
  appointments: [{id, cid, si, at, note, mtg}],  // mtg: Web会議URL（任意、2026-06-19追加）
  _cid, _hid, _aid
}
```
※`contacts`に「対応後ステータス」フィールドは無い。`saveContact()`で`contactStatus`を選ぶと顧客側の`status`だけが更新される（contact自体には残らない）。CRM自動転記はこの顧客`status`を見ている。

### 定数
```js
ROLES    = ['代表取締役','取締役','部長','課長','係長','担当者','一般','その他']
METHODS  = ['アウトバウンド','インバウンド','メール対応','OLS','対面','その他']
REASONS  = ['折返し連絡','クレーム対応','新規提案','その他']
STATUSES = ['未対応','優先対応','対応中','完了']
```

### 顧客ステータス色
| ステータス | CSSクラス | 色 |
|---|---|---|
| 未対応 | `.bd` | 赤 |
| 優先対応 | `.bp` | オレンジ枠 |
| 対応中 | `.bw` | 黄 |
| 完了 | `.bs` | 緑 |

### 画面構成（横タブナビ）

| 画面 | pg値 | 概要 |
|---|---|---|
| CRM ポータル | `portal` | 担当割り当てを季節カードで表示 |
| 顧客一覧 | `customers` | 検索・ステータスフィルタ・新規登録 |
| 顧客詳細 | `detail` | 左：次回アポ→顧客情報→アポ設定。右：対応入力フォーム＋履歴タイムライン |
| 設定（管理者のみ） | `settings` | 商材管理・担当割り当て・データ管理の3タブ |

### 主要関数
```js
render()          // セッション確認 → login/app HTML を描画 → bind()
portalHTML()      // 季節カードグリッド
customersHTML()   // 顧客一覧
detailHTML()      // 顧客詳細＋アポ表示＋対応入力フォーム＋タイムライン
settingsHTML()    // 設定
saveContact()     // 対応履歴保存（contactStatus で顧客status更新）
saveCust()        // 顧客新規登録・編集
saveApo()         // アポイント設定・更新
deleteApo()       // アポイント削除
lookupZip()       // 郵便番号 → zipcloud API → 住所自動補完
apoWatchdog IIFE  // 全3ファイル末尾に共通。30秒ごとにアポ監視
```

---

## アポイントアラート仕様（全3ファイル共通）

```
定数:
  CRM_KEY = 'crmCustomerState_v1'
  DIS_KEY = '_apoDismissed'（sessionStorage）
  BID     = '_apoBanner'（DOM要素ID）
  TWENTY  = 20分（ms）

表示条件:
  - アポ時刻の20分前 〜 1分経過後まで表示
  - sessionStorage[DIS_KEY] に含まれるIDは除外
  - staff: a.si === sess.user のもののみ
  - admin: 全件
```

---

## kpi_system.html 実装済み機能

### ページ構成（サイドバーナビ）

| ページ | id | 概要 |
|--------|-----|------|
| 打刻 | attendance | 出勤・休憩・退勤の4ボタン。残業申請・有給申請フォーム付き |
| 出勤履歴 | history | 日別打刻一覧 / 総合時間集計の2タブ。CSV出力可 |
| KPI入力 | kpi | 活動指標10項目・率指標8項目をカレンダー軸で入力 |
| 時間帯集計 | timeslot | 区切り時刻を自由追加・削除 |
| コールセンター | cc | 着信数/応答数/通話時間を日次入力 → 応答率/放棄率/AHT自動計算 |
| チャット報告 | report | LINE/Chatwork/Slackフォーマット切替 |
| 個人ダッシュボード | dashboard | 前月比カード。全KPI達成状況（月末予測列付き）|
| チームダッシュボード | team | アラートパネル。目標vs着地予測カード |
| 業績サマリー | summary | アウトバウンド・インバウンド統合サマリー |
| 着地タイムライン | timeline | KPIタイムラインバー表示 |
| チームメモ帳 | memo | 全スタッフ共有メモ |
| 管理者画面 | admin | 代理打刻 / 打刻修正 / 有給欠勤 / 目標設定 / 残業承認 / シフト / 給与 / スタッフ管理 |

---

## 開発ルール・お作法（kpi_system.html）

- **render() を基本単位**として扱う。状態変更後は必ず `render()` を呼ぶ
- **confirm() / alert() / prompt() は使わない** — toast / インラインバー で代替
- **innerHTMLへ入れる自由テキストは必ず `esc()`**
- **スタッフはindex（si）で全データを参照**
- 率指標は必ず **Math.min(100, ...)** で100%クランプ

## あなたへの指示

このプロジェクトは **コールセンター・営業KPI管理システム + 顧客管理CRM** の開発です。
既存ファイルを引き継いで開発を継続してください。
